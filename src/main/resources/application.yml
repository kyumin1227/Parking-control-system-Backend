spring.application.name: parking_control_system

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/pcs_dev?serverTimezone=Asia/Seoul"
    username: "kyumin"
    password: "1227"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect